/**
 * This example implements a <i>visitor pattern</i> in an automatic way by
 * using a processor that introduces an accept method in a visited type
 * hierarchy.
 */
package spoon.examples.visitor.src;
